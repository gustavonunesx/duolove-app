import { View, Text } from 'react-native';

export default function LoginScreen() {
  return (
    <View className="flex-1 bg-surface items-center justify-center">
      <Text className="text-text-primary text-2xl font-bold">DuoLove</Text>
      <Text className="text-text-muted mt-2">Tela de login — em breve</Text>
    </View>
  );
}
