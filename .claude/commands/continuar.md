Leia os arquivos CLAUDE.md e docs/PLAN.md para entender o estado atual do projeto.

Em seguida:
1. Identifique qual é a próxima milestone pendente (⬜) na tabela de milestones do CLAUDE.md
2. Leia a seção correspondente no docs/PLAN.md para ver as entregas detalhadas
3. Verifique o estado do git (branch atual, PRs abertos) com `git status` e `gh pr list`
4. Se houver PR aberto, faça o merge antes de começar
5. Apresente um resumo do que será feito na milestone e inicie a execução

Não pergunte confirmação — apenas execute.
